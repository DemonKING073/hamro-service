import React from "react"
import MainTemplate from "../components/MainTemplate"

const Products = () => {
    return(
        <MainTemplate>
            This is Products
        </MainTemplate>
    )
}

export default Products